#! usr/bin/python
"""This script is a Python implementation of union-find, which is an efficient
way to determine if two nodes are in the same connected component. This is
created for use with Python's Networkx graph library.

NOTE: This is WIP."""

import networkx

def find(x):
    """Finds the root node of the component containing the node x.

    Args:
        x: Node which we want to find the root node of the component for.
    Returns:
        Root node of the component containing x.
    """
    pass
